interface:
  display_name: "Detect Hermes Recommendation Poisoning"
  short_description: "Prefilter Hermes memory records with DeepSeek Pro, then review candidates."
  default_prompt: "Use $detect-recommendation-poisoning to run the DeepSeek Pro Python prefilter on my Hermes USER.md-style memory files, review suspicious or uncertain candidates, and write a Markdown findings report."
