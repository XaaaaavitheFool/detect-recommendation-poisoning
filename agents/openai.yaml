interface:
  display_name: "Detect Hermes Recommendation Poisoning"
  short_description: "Review Hermes memory for recommendation poisoning."
  default_prompt: "Use $detect-recommendation-poisoning to run the local Qwen prefilter on my Hermes USER.md-style memory files and write a Markdown findings report."
